class CurrencyDefinition:

    def __init__(
        self,
        key,
        currency_id,
        display_name,
    ):
        self.key = key
        self.currency_id = currency_id
        self.display_name = display_name